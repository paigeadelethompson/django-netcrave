"""Django application configuration for netcrave_config."""

from django.apps import AppConfig


class NetcraveConfigConfig(AppConfig):
    """Configuration for the netcrave_config application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "netcrave_config"
    verbose_name = "Netcrave Configuration"

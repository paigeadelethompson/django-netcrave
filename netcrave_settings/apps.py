"""Django application configuration for netcrave_settings."""

from django.apps import AppConfig


class NetcraveSettingsConfig(AppConfig):
    """Configuration for the netcrave_settings application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "netcrave_settings"
    verbose_name = "Netcrave Settings"

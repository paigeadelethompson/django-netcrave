"""Django application configuration for netcrave_ca."""

from django.apps import AppConfig


class NetcraveCaConfig(AppConfig):
    """Configuration for the netcrave_ca application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "netcrave_ca"
    verbose_name = "Netcrave Certificate Authority"
